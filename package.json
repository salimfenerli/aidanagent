{
  "name": "aidan",
  "version": "1.0.0",
  "private": true,
  "description": "Aidan - ADHD asistani. Kalici regresyon test suite.",
  "scripts": {
    "test": "node --test --test-concurrency=2 \"tests/*.test.js\"",
    "check": "node --check core.js && node --check tasks.js && node --check stocks.js && node --check ui.js && node --check program.js && node --check sw.js && node --check aidan-worker/worker.js"
  },
  "devDependencies": {
    "jsdom": "^25.0.0"
  }
}
